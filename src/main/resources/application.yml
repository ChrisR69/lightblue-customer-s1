server:
  context-path: /micro
  port: 8080

spring:
  cloudant:
    username: ${vcap.services.cloudant-s1.credentials.username}
    password: ${vcap.services.cloudant-s1.credentials.password}
    host: ${vcap.services.cloudant-s1.credentials.host}
    protocol: https
    port: 443
    database: customers